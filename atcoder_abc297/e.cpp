#include <bits/stdc++.h>
using namespace std;

int main(void){
    int n,k;
    cin >> n >> k;
    vector <int> a(n);
    set <unsigned long long> st;
    for(int i = 0;i < n; i++){
        cin >> a[i];
    }
    sort(a.begin(),a.end());
    while(st.size() != k){
        
    }
    return 0;
}